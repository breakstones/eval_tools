# LLM Eval Studio - 服务端口配置
# 此文件记录项目中所有服务的端口配置
# 修改端口时请同步更新此文件和相应配置文件

[前端服务]
PORT=5173
CONFIG_FILE=frontend/vite.config.ts
START_CMD=cd frontend && npm run dev

[后端服务]
PORT=8001
CONFIG_FILE=backend/app/config.py (默认无端口配置，由启动命令指定)
START_CMD=cd backend && uvicorn app.main:app --reload --port 8001

[代理配置]
# 前端通过 Vite proxy 代理到后端
API_PROXY=/api -> http://localhost:8001
WS_PROXY=/api/eval/ws -> http://localhost:8001

[启动顺序]
1. 先启动后端 (端口 8001)
2. 再启动前端 (端口 5173)

[快速启动命令]
# 后端
cd backend && uvicorn app.main:app --reload --port 8001

# 前端
cd frontend && npm run dev

[访问地址]
前端: http://localhost:5173
后端: http://localhost:8001
健康检查: http://localhost:8001/health
